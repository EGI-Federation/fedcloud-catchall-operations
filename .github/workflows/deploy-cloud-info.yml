---
name: 'Deploy'

on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - "deploy/**"

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      #dir: "deploy/cloud-info"
      dir: "foo/cloud-info"
      tags: "cloud-info,docker"
